let D=JSON.parse(localStorage.getItem('svms')||'{"products":[],"tx":[]}');const $=id=>document.getElementById(id);function save(){localStorage.setItem('svms',JSON.stringify(D))}function money(x){return '₹'+Number(x||0).toLocaleString('en-IN',{minimumFractionDigits:2,maximumFractionDigits:2})}function go(id){document.querySelectorAll('.page').forEach(x=>x.classList.remove('active'));$(id).classList.add('active');render();scrollTo(0,0)}function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function render(){let ps=D.products; $('pc').textContent=ps.length;$('uc').textContent=ps.reduce((a,p)=>a+p.stock,0);$('sv').textContent=money(ps.reduce((a,p)=>a+p.stock*p.buy,0));let day=new Date().toISOString().slice(0,10);$('ts').textContent=money(D.tx.filter(t=>t.type==='OUT'&&t.date.slice(0,10)===day).reduce((a,t)=>a+t.qty*t.price,0));$('low').innerHTML=ps.filter(p=>p.stock<=p.min).map(p=>`<div class="item"><span>${esc(p.name)}</span><b class="low">${p.stock}</b></div>`).join('')||'<div class="box">No low-stock products 🎉</div>';let q=($('search')?.value||'').toLowerCase();$('list').innerHTML=ps.filter(p=>(p.name+' '+p.cat+' '+p.bar).toLowerCase().includes(q)).map(p=>`<div class="item"><span><b>${esc(p.name)}</b><br><small>${esc(p.cat||'')} ${p.bar?'• '+esc(p.bar):''}<br>Buy ${money(p.buy)} • Sell ${money(p.sell)}</small></span><span><b class="${p.stock<=p.min?'low':''}">${p.stock}</b><br><button onclick="openP(${p.id})">Edit</button></span></div>`).join('')||'<div class="box">No products.</div>';['inP','outP'].forEach(id=>$(id).innerHTML=ps.map(p=>`<option value="${p.id}">${esc(p.name)} — Stock ${p.stock}</option>`).join(''));let sales=D.tx.filter(t=>t.type==='OUT');let rev=sales.reduce((a,t)=>a+t.qty*t.price,0);let cost=sales.reduce((a,t)=>{let p=ps.find(x=>x.id===t.pid);return a+t.qty*(p?.buy||0)},0);$('report').innerHTML=`Sales revenue: <b>${money(rev)}</b><br>Estimated gross profit: <b>${money(rev-cost)}</b><br>Current stock value: <b>${money(ps.reduce((a,p)=>a+p.stock*p.buy,0))}</b>`;$('tx').innerHTML=D.tx.slice(0,80).map(t=>{let p=ps.find(x=>x.id===t.pid);return `<div class="item"><span>${t.type==='OUT'?'🛒':'📥'} <b>${esc(p?.name||'Deleted')}</b><br><small>${new Date(t.date).toLocaleString('en-IN')} • Qty ${t.qty} • ${money(t.qty*t.price)} ${esc(t.note||'')}</small></span></div>`}).join('')||'<div class="box">No transactions.</div>'}
function openP(id){let p=D.products.find(x=>x.id===id);$('id').value=p?.id||'';$('name').value=p?.name||'';$('cat').value=p?.cat||'';$('bar').value=p?.bar||'';$('buy').value=p?.buy??'';$('sell').value=p?.sell??'';$('stock').value=p?.stock??0;$('stock').disabled=!!p;$('min').value=p?.min??5;$('modal').classList.add('open')}function closeP(){$('modal').classList.remove('open')}
function saveP(e){e.preventDefault();let id=Number($('id').value);let p={id:id||Date.now(),name:$('name').value.trim(),cat:$('cat').value.trim(),bar:$('bar').value.trim(),buy:+$('buy').value,sell:+$('sell').value,stock:+$('stock').value,min:+$('min').value};if(id){let i=D.products.findIndex(x=>x.id===id);p.stock=D.products[i].stock;D.products[i]=p}else{D.products.push(p);if(p.stock)D.tx.unshift({pid:p.id,type:'IN',qty:p.stock,price:p.buy,note:'Opening stock',date:new Date().toISOString()})}save();closeP();render()}
function move(e,type){e.preventDefault();let pid=+(type==='IN'?$('inP').value:$('outP').value),q=+(type==='IN'?$('inQ').value:$('outQ').value),note=type==='IN'?$('inN').value:$('outN').value,p=D.products.find(x=>x.id===pid);if(!p||q<=0)return alert('Enter valid details');if(type==='OUT'&&q>p.stock)return alert('Not enough stock. Available: '+p.stock);p.stock+=type==='IN'?q:-q;D.tx.unshift({pid,type,qty:q,price:type==='IN'?p.buy:p.sell,note,date:new Date().toISOString()});save();e.target.reset();render();alert(type==='IN'?'Stock added':'Sale saved')}
function backup(){let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(D)],{type:'application/json'}));a.download='svms_backup.json';a.click()}$('restore').onchange=e=>{let r=new FileReader();r.onload=()=>{try{let x=JSON.parse(r.result);if(!x.products||!x.tx)throw 0;D=x;save();render();alert('Backup restored')}catch{alert('Invalid backup')}};r.readAsText(e.target.files[0])};function wipe(){if(confirm('Delete all inventory data?')){D={products:[],tx:[]};save();render()}}go('home');